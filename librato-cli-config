#!/usr/bin/env node
var program = require('commander');
program
  .version('0.0.1')
  .command('settoken [token]', 'set the librato token for accessing the API')
  .command('setapikey [apiKey]', 'set the librato API key for accessing the API')
  .parse(process.argv);
